# Normalize all text files to LF in the repo and on checkout, independent of
# each contributor's core.autocrlf setting.
* text=auto eol=lf

# Binaries
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
